- name: Core packages

  packages:

  - repo: holoviz/panel
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz/hvplot
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz/datashader
    branch: main
    site: datashader.org
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz/geoviews
    branch: main
    site: geoviews.org
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz/holoviews
    branch: main
    site: holoviews.org
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, site, release-date
    gha_workflow: test.yaml
  
  - repo: holoviz/colorcet
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml
  
  - repo: holoviz/param
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz/lumen
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

- name: Extension packages

  packages:

  - repo: holoviz/holonote
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz/spatialpandas
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, release-date
    gha_workflow: test.yaml

  - repo: holoviz/pyviz_comms
    branch: main
    badges: gha, release, pypi, conda, conda-forge, defaults, release-date
    gha_workflow: test.yaml

- name: Bokeh packages

  packages:

  - repo: bokeh/bokeh
    badges: gha, site, release, pypi, conda, conda-forge, defaults
    conda_channel: bokeh
    gha_workflow: bokeh-ci.yml
    branch: main
    site: bokeh.org

  - repo: bokeh/jupyter_bokeh
    branch: main
    badges: gha, release, pypi, conda, conda-forge, defaults
    gha_workflow: build.yml
    conda_channel: bokeh

  - repo: bokeh/ipywidgets_bokeh
    branch: main
    badges: gha, release, pypi, conda, conda-forge, defaults
    gha_workflow: ipywidgets-bokeh-ci.yml
    conda_channel: bokeh

  - repo: bokeh/bokeh-fastapi
    branch: main
    badges: release, pypi, conda, conda-forge, defaults
    conda_channel: bokeh

  - repo: bokeh/bokeh-django
    branch: main
    badges: release, pypi, conda, conda-forge, defaults
    conda_channel: bokeh


- name: Metapackages, Example packages, and Websites only

  packages:

  - repo: holoviz/holoviz
    branch: main
    site: holoviz.org
    badges: gha, release, pypi, conda, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml

  - repo: holoviz-topics/examples
    branch: main
    badges: gha, gh-pages, site, docs-date
    gha_workflow: test.yml
    site: examples.pyviz.org

  - repo: holoviz-dev/blog
    branch: main
    badges: site
    site: blog.pyviz.org

  - repo: pyviz/pyviz.org
    branch: master
    badges: gha, site, docs-date
    site: pyviz.org
    gha_workflow: docs.yml

  - repo: holoviz-dev/status-dashboard
    branch: main
    badges: gha, site, docs-date
    site: status.holoviz.org
    gha_workflow: docs.yaml

  - repo: holoviz-topics/EarthML
    branch: main
    badges: site, docs-date
    site: earthml.holoviz.org

  - repo: holoviz-topics/EarthSim
    branch: master
    badges: release, conda-forge, gh-pages, site, release-date, docs-date

  - repo: holoviz-topics/imagen
    branch: master
    badges: release, pypi, gh-pages, site, release-date, docs-date

- name: Tooling and Infrastructure packages

  packages:

  - repo: holoviz-dev/nbsite
    branch: main
    badges: gha, release, pypi, conda, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml

  - repo: holoviz-dev/nbsmoke
    branch: main
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, release-date
    gha_workflow: tests.yaml

  - repo: holoviz-dev/autover
    branch: main
    badges: gha, release, pypi

  - repo: holoviz-dev/pyct
    branch: main
    badges: gha, release, pypi, conda, conda-forge, defaults, release-date

  - repo: holoviz-dev/pyctdev
    branch: main
    badges: gha, release, pypi, conda, pre-release-date

  - repo: holoviz-dev/sphinx_holoviz_theme
    branch: main
    badges: gha, release, pypi, conda, release-date

  - repo: holoviz/jupyter-panel-proxy
    branch: main
    badges: gha, release, pypi, conda, conda-forge, defaults
    gha_workflow: test.yaml

- name: Associated packages

  packages:

  - repo: dask/dask
    badges: gha, codecov, rtd, release, pypi, conda-forge, defaults
    gha_workflow: tests.yml
    branch: main

  - repo: numpy/numpy
    badges: gha, codecov, site, release, pypi, conda-forge, defaults, release-date
    gha_workflow: build_test.yml
    branch: main
    site: numpy.org

  - repo: pandas-dev/pandas
    badges: gha, codecov, site, release, pypi, conda-forge, defaults, release-date
    gha_workflow: python-dev.yml
    site: pandas.pydata.org

  - repo: pydata/xarray
    badges: gha, codecov, rtd, release, pypi, conda-forge, defaults, release-date
    gha_workflow: ci.yaml
    branch: main
    rtd_name: xray
