- name: Core and Coordinated packages

  packages:

  - repo: holoviz/panel
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml
    gha_branch: master

  - repo: holoviz/hvplot
    badges: gha, overalls, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml
    gha_branch: master

  - repo: holoviz/datashader
    site: datashader.org
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml
    gha_branch: master

  - repo: holoviz/geoviews
    site: geoviews.org
    badges: gha, coveralls, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yaml
    gha_branch: master

  - repo: holoviz/holoviews
    site: holoviews.org
    badges: gha, coveralls, release, pypi, conda, conda-forge, defaults, site, release-date
    gha_workflow: test.yml
    gha_branch: master
  
  - repo: holoviz/colorcet
    badges: appveyor, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml
    gha_branch: master
  
  - repo: holoviz/param
    badges: gha, coveralls, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml
    gha_branch: master

  - repo: holoviz/lumen
    badges: gha, codecov, release, pypi, conda, conda-forge, defaults, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml
    gha_branch: master

- name: Metapackages and Example packages

  packages:

  - repo: pyviz-topics/imagen
    badges: release, pypi, gh-pages, site, release-date, docs-date

  - repo: holoviz/holoviz
    site: holoviz.org
    badges: gha, release, pypi, conda, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml
    gha_branch: master

  - repo: pyviz-topics/EarthSim
    appveyor_project: pyviz/EarthSim
    badges: appveyor, release, conda-forge, gh-pages, site, release-date, docs-date

  - repo: pyviz-topics/EarthML
    appveyor_project: jsignell/earthml
    badges: appveyor, gh-pages, site, release-date, docs-date

  - repo: pyviz-topics/examples
    badges: gha, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml
    gha_branch: master

- name: Tooling and Infrastructure packages

  packages:

  - repo: pyviz-dev/sphinx_holoviz_theme
    badges: release, pypi, conda, release-date

  - repo: pyviz-dev/nbsite
    badges: gha, release, pypi, conda, gh-pages, site, release-date, docs-date
    gha_workflow: test.yml
    gha_branch: master

  - repo: pyviz-dev/nbsmoke
    appveyor_project: pyviz/nbsmoke
    badges: appveyor, coveralls, release, pypi, conda, conda-forge, defaults, release-date

  - repo: pyviz-dev/autover
    appveyor_project: pyviz/autover
    badges: appveyor, release, pypi, release-date

  - repo: pyviz-dev/pyct
    badges: release, pypi, conda, conda-forge, defaults, release-date

  - repo: pyviz-dev/pyctdev
    badges: release, pypi, conda, pre-release-date

- name: Associated packages

  packages:

  - repo: bokeh/bokeh
    badges: gha, release, pypi, conda, conda-forge, defaults
    conda_channel: bokeh
    gha_workflow: bokeh-ci.yml
    gha_branch: main

  - repo: dask/dask
    badges: gha, coveralls, rtd, release, pypi, conda-forge, defaults
    gha_workflow: tests.yml
    gha_branch: main

  - repo: numpy/numpy
    appveyor_project: charris/numpy
    badges: gha, appveyor, codecov, rtd, release, pypi, conda-forge, defaults, release-date
    gha_workflow: build_test.yml
    gha_branch: main

  - repo: pandas-dev/pandas
    badges: gha, codecov, rtd, release, pypi, conda-forge, defaults, release-date
    gha_workflow: python-dev.yml
    gha_branch: master

  - repo: pydata/xarray
    appveyor_project: shoyer/xray
    badges: gha, coveralls, rtd, release, pypi, conda-forge, defaults, release-date
    gha_workflow: ci.yaml
    gha_branch: main

- name: Data Ingestion packages

  packages:

  - repo: ContinuumIO/intake
    badges: gha, rtd, release, pypi, conda, conda-forge, defaults
    pypi_name: intake
    conda_channel: intake
    gha_workflow: main.yaml
    gha_branch: master

  - repo: ContinuumIO/intake-xarray
    badges: gha, rtd, release, pypi, conda, conda-forge, defaults
    conda_channel: intake
    gha_workflow: main.yaml
    gha_branch: master

  - repo: ContinuumIO/intake-parquet
    conda_channel: intake
    badges: rtd, release, pypi, conda, conda-forge

  - repo: zarr-developers/zarr
    badges: gha, coveralls, rtd, release, pypi, conda-forge, defaults, release-date
    gha_workflow: python-package.yml
    gha_branch: master

  - repo: dask/fastparquet
    badges: gha, rtd, release, pypi, conda-forge, defaults
    gha_workflow: main.yaml
    gha_branch: main

  - repo: Unidata/netcdf4-python
    pypi_name: netCDF4
    conda_package: netCDF4
    badges: gha, release, pypi, conda-forge, defaults, release-date
    gha_workflow: miniconda.yml
    gha_branch: master


- name: Niche packages

  packages:

  - repo: networkx/networkx
    appveyor_project: dschult/networkx-pqott
    badges: gha, codecov, release, pypi, conda-forge, defaults, release-date
    gha_workflow: test.yml
    gha_branch: main

  - repo: scikit-image/scikit-image
    badges: gha, codecov, release, pypi, conda-forge, defaults, release-date
    gha_workflow: tests.yml
    gha_branch: main

  - repo: SciTools/cartopy
    badges: gha, coveralls, release, pypi, conda-forge, defaults, release-date
    gha_workflow: ci-testing.yml
    gha_branch: master

  - repo: geopandas/geopandas
    badges: gha, coveralls, rtd, release, pypi, conda-forge, defaults, release-date
    gha_workflow: tests.yaml
    gha_branch: master
